# Gemini 3.5 Flash API Node.js Example

Run this backend-only example from the repo root or from this directory.

```bash
cp ../.env.example ../.env
cd node
npm start
```

Keep `POYO_API_KEY` on the server. Do not put it in browser code or public client bundles.
